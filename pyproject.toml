[project]
name = "airflow-beginners"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Asif Tandel", email = "asiftandel96@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "apache-airflow>=3.3.1",
]

[project.scripts]
airflow-beginners = "airflow_beginners:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
